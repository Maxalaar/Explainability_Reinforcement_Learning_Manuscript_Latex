\begin{figure}[t]
    \centering
    \includegraphics[width=0.85\textwidth]
        {figures/latent_space/sparse_autoencoder_illustration.png}
    \caption{
        Example of a feature learned by a sparse autoencoder applied to a large language model \cite{templeton2024scaling}.
        The highlighted tokens show the contexts in which the feature activates strongly, revealing a coherent semantic concept across English text, multiple languages, and images.
        This multimodal consistency suggests that the learned feature corresponds to a meaningful and generalizable concept, here related to the Golden Gate Bridge, rather than a spurious statistical pattern.
    }
    \label{fig:sparse_autoencoder_illustration}
\end{figure}