\begin{figure}[t]
    \centering
    \includegraphics[width=0.85\textwidth]
        {figures/latent_space/sparse_autoencoder_illustration.png}
    \caption{
        A concept direction corresponding to the Golden Gate Bridge, discovered
        by a sparse autoencoder applied to a large language model
        \cite{templeton2024scaling}.
        This direction activates strongly whenever the Golden Gate Bridge is
        evoked in the input, whether through English text, other languages, or
        even images, demonstrating that the learned concept is not tied to a
        specific surface form but captures a deeper semantic abstraction shared
        across modalities.
    }
    \label{fig:sparse_autoencoder_illustration}
\end{figure}