% ============================================================
% Parameters
% ============================================================

% General text size (in pt)
\providecommand{\NeuralArchTextSize}{14}

% Size of labels A and B only (in pt)
\providecommand{\NeuralArchLabelSize}{18}

% Horizontal shift of heads A and B
\providecommand{\NeuralArchBoxShiftX}{0.2cm}

% Margins around boxes A and B
\providecommand{\NeuralArchBoxMarginX}{2cm}
\providecommand{\NeuralArchBoxMarginY}{2cm}

% Radius of the circles around A and B
\providecommand{\NeuralArchCircleRadius}{0.18cm}

% Offset of the A/B circle relative to the label position
\providecommand{\NeuralArchCircleOffsetX}{0cm}
\providecommand{\NeuralArchCircleOffsetY}{0cm}

% ============================================================
% Figure
% ============================================================

\begin{figure}[t]

\centering

\resizebox{\textwidth}{!}{

\begin{tikzpicture}

% --------------------------------------------------------
% General text size
% --------------------------------------------------------

\fontsize{\NeuralArchTextSize pt}
         {\dimexpr\NeuralArchTextSize pt*6/5\relax}
\selectfont


% --------------------------------------------------------
% Observation
% --------------------------------------------------------

\pic[shift={(0,0,0)}] at (0,0,0)
    {Box={
        name=obs,
        caption=Observation,
        xlabel={{"4",""}},
        zlabel=,
        fill=ObservationColor,
        height=42,
        width=6,
        depth=42
    }};


% --------------------------------------------------------
% Convolutional trunk
% --------------------------------------------------------

\pic[shift={(2,0,0)}] at (obs-east)
    {Box={
        name=c1,
        caption=Conv,
        xlabel={{"16",""}},
        zlabel=,
        fill=ConvColor,
        height=34,
        width=6,
        depth=34
    }};

\draw [connection] (obs-east) -- node {\midarrow} (c1-west);


\pic[shift={(1.5,0,0)}] at (c1-east)
    {Box={
        name=c2,
        caption=Conv,
        xlabel={{"32",""}},
        zlabel=,
        fill=ConvColor,
        height=26,
        width=9,
        depth=26
    }};

\draw [connection] (c1-east) -- node {\midarrow} (c2-west);


\pic[shift={(1.5,0,0)}] at (c2-east)
    {Box={
        name=c3,
        caption=Conv,
        xlabel={{"64",""}},
        zlabel=,
        fill=ConvColor,
        height=18,
        width=12,
        depth=18
    }};

\draw [connection] (c2-east) -- node {\midarrow} (c3-west);


\pic[shift={(1.5,0,0)}] at (c3-east)
    {Box={
        name=c4,
        caption=Conv,
        xlabel={{"128",""}},
        zlabel=,
        fill=ConvColor,
        height=12,
        width=16,
        depth=12
    }};

\draw [connection] (c3-east) -- node {\midarrow} (c4-west);


\node[align=center] at ($(c4-east)+(0.9,0)$) {flatten};


% ============================================================
% Head A
% ============================================================

\pic[
    shift={(\NeuralArchBoxShiftX,3.2,0)}
] at (c4-east)
    {Box={
        name=aFc1,
        caption={Dense 128},
        fill=FcColor,
        opacity=0.7,
        height=3,
        width=2,
        depth=16
    }};


\pic[shift={(2,0,0)}] at (aFc1-east)
    {Box={
        name=aOut,
        caption=,
        fill=OutputColor,
        opacity=0.8,
        height=3,
        width=3,
        depth=8
    }};


\node[align=center]
    at ($(aOut-south)+(0,-1.2)$)
    {\bfseries Distribution};


\draw [connection]
    (aFc1-east) -- node {\midarrow} (aOut-west);


% ------------------------------------------------------------
% Box A
% ------------------------------------------------------------

\node[
    draw,
    dashed,
    rounded corners=0.1cm,
    inner xsep=\NeuralArchBoxMarginX,
    inner ysep=\NeuralArchBoxMarginY,
    fit=(aFc1-nearnorthwest)
        (aFc1-farsouthwest)
        (aOut-nearsoutheast)
        (aOut-farnortheast)
] (boxA) {};


% ------------------------------------------------------------
% Label A + circle
% ------------------------------------------------------------

\node[
    above left=-0.1cm and -0.1cm of boxA.north west,
    inner sep=0pt,
    font=\fontsize{\NeuralArchLabelSize pt}
              {\dimexpr\NeuralArchLabelSize pt*6/5\relax}
              \selectfont\bfseries
] (labelA) {A};


\draw[
    thick
]
($(labelA.center)
    +(\NeuralArchCircleOffsetX,\NeuralArchCircleOffsetY)$)
circle
    (\NeuralArchCircleRadius);


% ============================================================
% Head B
% ============================================================

\pic[
    shift={(\NeuralArchBoxShiftX,-3.2,0)}
] at (c4-east)
    {Box={
        name=bFc1,
        caption={Dense 512},
        fill=FcColor,
        opacity=0.7,
        height=3,
        width=2,
        depth=30
    }};


\pic[shift={(1.5,0,0)}] at (bFc1-east)
    {Box={
        name=bFc2,
        caption={Dense 256},
        fill=FcColor,
        opacity=0.7,
        height=3,
        width=2,
        depth=22
    }};

\draw [connection]
    (bFc1-east) -- node {\midarrow} (bFc2-west);


\pic[shift={(1.5,0,0)}] at (bFc2-east)
    {Box={
        name=bFc3,
        caption={Dense 128},
        fill=FcColor,
        opacity=0.7,
        height=3,
        width=2,
        depth=16
    }};

\draw [connection]
    (bFc2-east) -- node {\midarrow} (bFc3-west);


\pic[shift={(1.5,0,0)}] at (bFc3-east)
    {Box={
        name=bOut,
        caption=,
        fill=OutputColor,
        opacity=0.8,
        height=3,
        width=3,
        depth=8
    }};


\node[align=center]
    at ($(bOut-south)+(0,-1.2)$)
    {\bfseries Distribution};


\draw [connection]
    (bFc3-east) -- node {\midarrow} (bOut-west);


% ------------------------------------------------------------
% Box B
% ------------------------------------------------------------

\node[
    draw,
    dashed,
    rounded corners=0.1cm,
    inner xsep=\NeuralArchBoxMarginX,
    inner ysep=\NeuralArchBoxMarginY,
    fit=(bFc1-nearnorthwest)
        (bFc1-farsouthwest)
        (bOut-nearsoutheast)
        (bOut-farnortheast)
] (boxB) {};


% ------------------------------------------------------------
% Label B + circle
% ------------------------------------------------------------

\node[
    above left=-0.1cm and -0.1cm of boxB.north west,
    inner sep=0pt,
    font=\fontsize{\NeuralArchLabelSize pt}
              {\dimexpr\NeuralArchLabelSize pt*6/5\relax}
              \selectfont\bfseries
] (labelB) {B};


\draw[
    thick
]
($(labelB.center)
    +(\NeuralArchCircleOffsetX,\NeuralArchCircleOffsetY)$)
circle
    (\NeuralArchCircleRadius);


% ============================================================
% Legend
% ============================================================

\node[
    align=center,
    anchor=north,
    yshift=-0.3cm
] at (current bounding box.south) {

    % Circle A
    \tikz[baseline=-0.6ex]{
        \node[
            circle,
            draw,
            inner sep=0pt,
            minimum size=2*\NeuralArchCircleRadius,
            font=\fontsize{\NeuralArchLabelSize pt}
                      {\dimexpr\NeuralArchLabelSize pt*6/5\relax}
                      \selectfont\bfseries
        ] {A};
    }
    \quad Pong, Space Invaders
    \qquad\qquad

    % Circle B
    \tikz[baseline=-0.6ex]{
        \node[
            circle,
            draw,
            inner sep=0pt,
            minimum size=2*\NeuralArchCircleRadius,
            font=\fontsize{\NeuralArchLabelSize pt}
                      {\dimexpr\NeuralArchLabelSize pt*6/5\relax}
                      \selectfont\bfseries
        ] {B};
    }
    \quad Breakout, Mario Bros, Ms.\ Pac-Man, Surround

};


\end{tikzpicture}

}

\caption{
    Architecture of the surrogate policies.
}

\label{fig:neural_architecture}

\end{figure}
